import "package:flutter/material.dart";
import "package:get/get.dart";

class SupportMenu extends StatelessWidget {
  const SupportMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Clients Menu"),
    );
  }
}